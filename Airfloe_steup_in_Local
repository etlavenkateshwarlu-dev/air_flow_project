Airflow Local Setup
------------------
Pre-requisites:WSL installed
Docker Desktop

Steps:
Define a Dockerfile to customize the Airflow image

Build a Docker image using the Dockerfile

Configure services using Docker Compose

docker-compose.yaml file

Launch Airflow using Docker Compose